{
  "name": "@aidlc/core",
  "version": "0.0.1",
  "description": "AIDLC core engine — workspace loader, runner registry, pipeline executor. Pure TypeScript (no vscode dep).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "templates"
  ],
  "scripts": {
    "compile": "tsc -p ./",
    "build": "tsc -p ./",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/delete101020/ai-native-sdlc"
  },
  "dependencies": {
    "js-yaml": "^4.1.1",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.19.39",
    "vitest": "^4.1.5"
  }
}
