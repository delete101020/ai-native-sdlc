{
  "name": "aidlc",
  "version": "3.5.0",
  "description": "AIDLC terminal CLI — AI-driven SDLC pipeline runner. Plan → Prototype → Design ∥ Test → Implement → Release with discovery gates, prototype phase, requirements traceability, and Spec Kit support.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/delete101020/ai-native-sdlc"
  },
  "homepage": "https://github.com/delete101020/ai-native-sdlc#readme",
  "bugs": {
    "url": "https://github.com/delete101020/ai-native-sdlc/issues"
  },
  "keywords": [
    "aidlc",
    "claude",
    "ai",
    "workflow",
    "sdlc",
    "agents",
    "cli"
  ],
  "engines": {
    "node": ">=18"
  },
  "main": "./dist/bundle.js",
  "bin": {
    "aidlc": "./dist/bundle.js"
  },
  "files": [
    "dist/bundle.js",
    "dist/templates",
    "dist/tools",
    "dist/vendor",
    "dist/assets",
    "README.md",
    "AUTOMATION.md"
  ],
  "scripts": {
    "compile": "tsc -p ./",
    "build": "tsc -p ./",
    "clean": "rm -rf dist",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=dist/bundle.js --external:fsevents && rm -rf dist/templates && cp -R ../core/templates dist/templates && rm -rf dist/tools dist/vendor dist/assets && cp -R ../../tools dist/tools && mkdir -p dist/vendor && cp -R ../../vendor/annotron dist/vendor/annotron && cp -R ../extension/assets dist/assets",
    "prepublishOnly": "pnpm bundle"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "devDependencies": {
    "@aidlc/core": "workspace:*",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "chalk": "^4.1.2",
    "chokidar": "^3.6.0",
    "cli-table3": "^0.6.5",
    "commander": "^11.1.0",
    "esbuild": "^0.21.0",
    "js-yaml": "^4.1.1"
  }
}
